const INITIAL_STATE={
    iduser : null,
    email : ""
}

const adminReducer =(state=INITIAL_STATE, action)=>{
    switch(action.type){
        default:
            return state
    }
}

export default adminReducer